name: Testi
on: [push]
jobs:
  ekatesti:
    runs-on: ubuntu-latest
    steps:
      - name: Komento
        run: |
          echo "Commit on $github.event.commits[0].message"
          echo "The nam eis ${{vars.MY_NAME}}"

          
