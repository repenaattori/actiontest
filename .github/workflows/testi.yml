name: Testi
on: [workflow_dispatch]
jobs:
  ekatesti:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Komento
        run: git log --oneline
